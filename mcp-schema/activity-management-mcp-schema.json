[
  {
    "name": "AddActivities",
    "description": "指定した日に新しい活動を追加する（既存の活動は保持される）。1つまたは複数の活動を追加可能。例：「今日は7時に起床、8時に朝食を記録」「12時に昼食を追加」など。",
    "inputSchema": {
      "type": "object",
      "properties": {
        "operationType": {
          "type": "string",
          "description": "操作タイプ。このツールでは常に'append'を指定してください"
        },
        "userId": {
          "type": "string",
          "description": "ユーザーID"
        },
        "date": {
          "type": "string",
          "description": "日付（YYYY-MM-DD）"
        },
        "activities": {
          "type": "array",
          "description": "追加する活動のリスト（1つ以上）",
          "items": {
            "type": "object",
            "properties": {
              "time": {
                "type": "string",
                "description": "時刻（HH:MM）"
              },
              "activityType": {
                "type": "string",
                "description": "活動タイプ（wakeUp|sleep|exercise|meal|snack|mood|medication|bowelMovement|urination|symptoms|other）"
              },
              "description": {
                "type": "string",
                "description": "活動の説明"
              },
              "items": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "活動の具体的な内容リスト。食事の場合は食べた物と量（例：[\"おにぎり2個\", \"唐揚げ4個\", \"味噌汁1杯\"]）、運動の場合は運動内容と回数・時間（例：[\"腕立て伏せ20回\", \"ランニング30分\"]）など、具体的な詳細を記録するために利用してください。細かく登録しようとしすぎるとユーザが嫌になってしまうので、ユーザの申告した内容レベルを尊重してください。"
              }
            },
            "required": ["time", "activityType", "description"]
          }
        }
      },
      "required": ["operationType", "userId", "date", "activities"]
    }
  },
  {
    "name": "UpdateActivity",
    "description": "指定したactivityIdの活動を部分的に更新する。例：「朝食の時刻を8時30分に変更」「昼食の内容を修正」など、1つの活動のみを修正する場合に使用。",
    "inputSchema": {
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "ユーザーID"
        },
        "date": {
          "type": "string",
          "description": "活動の日付（YYYY-MM-DD）"
        },
        "activityId": {
          "type": "string",
          "description": "更新対象の活動ID"
        },
        "time": {
          "type": "string",
          "description": "時刻（HH:MM）"
        },
        "activityType": {
          "type": "string",
          "description": "活動タイプ（wakeUp|sleep|exercise|meal|snack|mood|medication|bowelMovement|urination|symptoms|other）"
        },
        "description": {
          "type": "string",
          "description": "活動の説明"
        },
        "items": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "活動の具体的な内容リスト。食事の場合は食べた物と量（例：[\"おにぎり2個\", \"唐揚げ4個\", \"味噌汁1杯\"]）、運動の場合は運動内容と回数・時間（例：[\"腕立て伏せ20回\", \"ランニング30分\"]）など、具体的な詳細を記録するために利用してください。細かく登録しようとしすぎるとユーザが嫌になってしまうので、ユーザの申告した内容レベルを尊重してください。"
        }
      },
      "required": ["userId", "date", "activityId"]
    }
  },
  {
    "name": "UpdateActivities",
    "description": "指定した日の全ての活動を完全に置き換える。既存の活動は全て削除され、新しいリストで上書きされる。例：「今日の記録を全部やり直したい」「複数の活動をまとめて修正」など、大幅な変更が必要な場合のみ使用。単一の活動の修正にはUpdateActivityを使用すること。",
    "inputSchema": {
      "type": "object",
      "properties": {
        "operationType": {
          "type": "string",
          "description": "操作タイプ。このツールでは常に'replace'を指定してください"
        },
        "userId": {
          "type": "string",
          "description": "ユーザーID"
        },
        "date": {
          "type": "string",
          "description": "日付（YYYY-MM-DD）"
        },
        "activities": {
          "type": "array",
          "description": "新しい活動のリスト（既存の活動は全て置き換えられます）",
          "items": {
            "type": "object",
            "properties": {
              "time": {
                "type": "string",
                "description": "時刻（HH:MM）"
              },
              "activityType": {
                "type": "string",
                "description": "活動タイプ（wakeUp|sleep|exercise|meal|snack|mood|medication|bowelMovement|urination|symptoms|other）"
              },
              "description": {
                "type": "string",
                "description": "活動の説明"
              },
              "items": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "活動の具体的な内容リスト。食事の場合は食べた物と量（例：[\"おにぎり2個\", \"唐揚げ4個\", \"味噌汁1杯\"]）、運動の場合は運動内容と回数・時間（例：[\"腕立て伏せ20回\", \"ランニング30分\"]）など、具体的な詳細を記録するために利用してください。細かく登録しようとしすぎるとユーザが嫌になってしまうので、ユーザの申告した内容レベルを尊重してください。"
              }
            },
            "required": ["time", "activityType", "description"]
          }
        }
      },
      "required": ["operationType", "userId", "date", "activities"]
    }
  },
  {
    "name": "DeleteActivity",
    "description": "指定したactivityIdの活動を削除する。例：「朝食を削除」「この記録を消して」など。",
    "inputSchema": {
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "ユーザーID"
        },
        "date": {
          "type": "string",
          "description": "活動の日付（YYYY-MM-DD）"
        },
        "activityId": {
          "type": "string",
          "description": "削除対象の活動ID"
        }
      },
      "required": ["userId", "date", "activityId"]
    }
  },
  {
    "name": "GetActivities",
    "description": "指定した日のユーザーの行動を取得する",
    "inputSchema": {
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "ユーザーID"
        },
        "date": {
          "type": "string",
          "description": "日付（YYYY-MM-DD）"
        }
      },
      "required": ["userId", "date"]
    }
  },
  {
    "name": "GetActivitiesInRange",
    "description": "指定した期間のユーザーの行動履歴を取得する（最大365日間）",
    "inputSchema": {
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "ユーザーID"
        },
        "startDate": {
          "type": "string",
          "description": "開始日（YYYY-MM-DD）"
        },
        "endDate": {
          "type": "string",
          "description": "終了日（YYYY-MM-DD）"
        }
      },
      "required": ["userId", "startDate", "endDate"]
    }
  }
]